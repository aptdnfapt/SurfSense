# SurfSense Frontend Environment Configuration

# =============================================================================
# IMPORTANT: Runtime Configuration System (v0.0.8+)
# =============================================================================
# NEXT_PUBLIC_* variables are NO LONGER REQUIRED in production!
# The frontend now fetches configuration from backend at runtime via /api/v1/config
# This allows changing AUTH_TYPE, ETL_SERVICE, etc. without rebuilding images
#
# See DOCKER_RUNTIME_CONFIG_MIGRATION_PLAN.md for full details
# =============================================================================

# Backend URL (required for runtime config fetch)
# In production Docker: set this to your public backend URL
# For local development: use localhost
NEXT_PUBLIC_FASTAPI_BACKEND_URL=http://localhost:8000

# DEPRECATED: These are fetched from /api/v1/config at runtime
# Kept here for backward compatibility only (will be removed in future)
# NEXT_PUBLIC_FASTAPI_BACKEND_AUTH_TYPE=LOCAL or GOOGLE  # Use AUTH_TYPE in backend .env instead
# NEXT_PUBLIC_ETL_SERVICE=UNSTRUCTURED or LLAMACLOUD     # Use ETL_SERVICE in backend .env instead
# Contact Form Vars - OPTIONAL
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.sdsf.supabase.co:5432/postgres